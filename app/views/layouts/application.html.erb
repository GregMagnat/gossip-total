<!DOCTYPE html>
<html>
<head>
  <title>The Gossip Project</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css', media: 'all' %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body style="background-image: url('https://www.wallfizz.com/nature/ile-et-ile-deserte/6015-ile-au-paradis-WallFizz.jpg'); background-size: cover; background-repeat: no-repeat; background-attachment: fixed; background-color: rgba(0, 0, 0, 0.5);">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="<%= home_path %>">The Gossip Project</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Home", home_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Contact", contact_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Team", team_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "New Gossips", new_gossip_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Gossips", gossips_path, class: "nav-link" %>
          </li>
          <div class="navbar-nav ms-auto">
            <% if current_user %>
              <%= form_tag session_path(current_user), method: :delete, class: "nav-link" do %>
                <%= submit_tag "Logout" %>
              <% end %>
            <% else %>
              <%= link_to "Login", new_session_path, class: "nav-link" %>
            <% end %>
          </div>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <%= yield %>
  </div>
  <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.min.js' %>
</body>
</html>
